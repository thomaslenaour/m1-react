import { FC } from 'react';
import Layout from '../components/Layout';

const Favorites: FC = () => {
  return (
    <Layout>
      <div>Home Page</div>
    </Layout>
  );
};

export default Favorites;
